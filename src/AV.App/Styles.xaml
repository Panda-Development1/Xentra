<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== Brushes ===== -->
    <Color x:Key="C_BgBase">#0A0B10</Color>
    <Color x:Key="C_Accent">#22D3EE</Color>
    <Color x:Key="C_Accent2">#B96BFB</Color>
    <Color x:Key="C_Green">#34D399</Color>
    <Color x:Key="C_Red">#FB7185</Color>
    <Color x:Key="C_Text">#ECEEF5</Color>
    <Color x:Key="C_TextMuted">#8B91A3</Color>

    <SolidColorBrush x:Key="BgBase" Color="{StaticResource C_BgBase}"/>
    <SolidColorBrush x:Key="GlassFill" Color="#14151C" Opacity="0.55"/>
    <SolidColorBrush x:Key="GlassFillStrong" Color="#1B1D27" Opacity="0.72"/>
    <SolidColorBrush x:Key="BorderGlass" Color="#FFFFFF" Opacity="0.08"/>
    <SolidColorBrush x:Key="Accent" Color="{StaticResource C_Accent}"/>
    <SolidColorBrush x:Key="Accent2" Color="{StaticResource C_Accent2}"/>
    <SolidColorBrush x:Key="AccentSoft" Color="{StaticResource C_Accent}" Opacity="0.16"/>
    <SolidColorBrush x:Key="Green" Color="{StaticResource C_Green}"/>
    <SolidColorBrush x:Key="Red" Color="{StaticResource C_Red}"/>
    <SolidColorBrush x:Key="TextPrimary" Color="{StaticResource C_Text}"/>
    <SolidColorBrush x:Key="TextMuted" Color="{StaticResource C_TextMuted}"/>

    <!-- ===== Icon geometry (24x24 viewBox) ===== -->
    <Geometry x:Key="IconShield">M12 2 L20 5 V11 C20 16 16 20 12 22 C8 20 4 16 4 11 V5 Z</Geometry>
    <Geometry x:Key="IconScan">M10 2 A8 8 0 1 0 10 18 A8 8 0 1 0 10 2 M15.5 15.5 L21 21</Geometry>
    <Geometry x:Key="IconQuarantine">M4 7 L12 4 L20 7 V17 L12 20 L4 17 Z M4 7 L12 10 L20 7 M12 10 V20</Geometry>
    <Geometry x:Key="IconDashboard">M3 3 H11 V11 H3 Z M13 3 H21 V11 H13 Z M3 13 H11 V21 H3 Z M13 13 H21 V21 H13 Z</Geometry>
    <Geometry x:Key="IconRestore">M3 12 A9 9 0 1 1 4 16 M3 12 L3 7 M3 12 L8 12</Geometry>
    <Geometry x:Key="IconDelete">M4 7 H20 M9 7 V4 H15 V7 M6 7 L7 20 H17 L18 7</Geometry>
    <Geometry x:Key="IconMin">M3 11 H13</Geometry>
    <Geometry x:Key="IconMax">M4 4 H14 V14 H4 Z</Geometry>
    <Geometry x:Key="IconClose">M4 4 L14 14 M14 4 L4 14</Geometry>
    <Geometry x:Key="IconRefresh">M4 10 A8 8 0 1 1 3 14 M4 10 L4 5 M4 10 L9 10</Geometry>

    <!-- ===== Glass panel ===== -->
    <Style TargetType="Border" x:Key="GlassPanel">
        <Setter Property="Background" Value="{StaticResource GlassFill}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderGlass}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="14"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="26" ShadowDepth="0" Color="#000000" Opacity="0.40"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Glass button ===== -->
    <Style TargetType="Button" x:Key="GlassButton">
        <Setter Property="Background" Value="{StaticResource GlassFill}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderGlass}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="14,9"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="9" SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect x:Name="Glow" BlurRadius="0" ShadowDepth="0"
                                              Color="{StaticResource C_Accent}" Opacity="0"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}" RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentSoft}"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Glow" Storyboard.TargetProperty="Opacity"
                                                         To="0.55" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Glow" Storyboard.TargetProperty="Opacity"
                                                         To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentSoft}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Nav button ===== -->
    <Style TargetType="Button" x:Key="NavButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="14,11"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="9"
                            SnapsToDevicePixels="True" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentSoft}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Icon (title-bar) button ===== -->
    <Style TargetType="Button" x:Key="IconButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="38"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="7">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#FFFFFF" />
                            <Setter TargetName="Bd" Property="Opacity" Value="0.10"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Glass progress bar ===== -->
    <Style TargetType="ProgressBar" x:Key="GlassProgress">
        <Setter Property="Height" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border Background="#FFFFFF" Opacity="0.07" CornerRadius="6"/>
                        <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="6"
                                Background="{StaticResource Accent}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" ShadowDepth="0" Color="{StaticResource C_Accent}" Opacity="0.85"/>
                            </Border.Effect>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== ScrollBar ===== -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid>
                        <Track Name="PART_Track" IsDirectionReversed="True">
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="#FFFFFF" Opacity="0.16" CornerRadius="4"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Section label ===== -->
    <Style TargetType="TextBlock" x:Key="SectionLabel">
        <Setter Property="Foreground" Value="{StaticResource Accent2}"/>
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="Heading">
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
</ResourceDictionary>
